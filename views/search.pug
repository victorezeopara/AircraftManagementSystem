doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Aircraft Management System - Search
    link(href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap", rel="stylesheet")
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css")
    style.
      /* Embedded CSS styles */
      body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        padding: 0;
        background-image: linear-gradient(135deg, rgba(102, 126, 234, 0.4) 50%, rgba(118, 75, 162, 0.8) 30%), url('/images/jets.png');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .form_container_search {
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        padding: 20px;
        width: 100%;
        max-width: 400px;
        text-align: center;
      }

      .validated-form {
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      .content1 {
        margin-bottom: 20px;
      }

      .input.search {
        width: 100%;
        padding: 10px;
        margin-top: 5px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
        font-size: 1rem;
      }

      .submit {
        background-color: #667eea;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1rem;
        transition: background-color 0.3s;
      }

      .submit:hover {
        background-color: #764ba2;
      }
  body
    .form_container_search
      form.validated-form(action="/search", method="POST")
        .content1
          br
          br
          label.label(for="search_key") Search Aircraft by
          br
          br
          select.input(name="search_by")
            option(value="aircraft_name") Aircraft Name
            option(value="aircraft_number") Aircraft Number
            option(value="phone") Phone Number
          br
          br
          input.input.search(type="text" name="search_key" placeholder="Aircraft name or number" required="required")
          br
          br
          button.submit(type="submit") Search
